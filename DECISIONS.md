# DECISIONS.md — approved deviations & spec-silent choices

Format: date · phase · decision · why.

- 2026-07-02 · P0 · **JUCE pinned to `8.0.14`** — latest stable 8.x tag on the JUCE repo at time of Phase 0.
- 2026-07-02 · P0 · **Toolchain = existing VS 2022 Community 17.14 + its bundled CMake 3.31.6 and Git** — everything the spec requires was already installed; nothing new was added. Builds are driven from WSL by invoking the Windows `cmake.exe`, so compilation is native MSVC exactly as specified.
- 2026-07-02 · P0 · **`Tools/` and `tools/` are the same directory** (NTFS is case-insensitive). The repo uses `Tools/` for render/tests sources *and* for `grant_vst3_write.ps1` + `pluginval.exe`, matching both spellings in CLAUDE.md.
- 2026-07-02 · P1 · **Master gain range −60..+6 dB, −60 displayed as "-inf dB", default 0 dB** — SPEC §10 says "−inf..+6 dB" without a floor; −60 dB is inaudible and keeps the range finite for the slider/automation.
- 2026-07-02 · P1 · **JUCE splash screen left enabled** — disabling it (`JUCE_DISPLAY_SPLASH_SCREEN=0`) is only licensed under AGPLv3 or a paid JUCE license. Needs a user decision on how Lumen will be licensed; until then the default splash stays (it briefly overlays the editor's bottom-right corner).
- 2026-07-02 · P1 · **`juce::OpenGLContext` attachment deferred to Phase 5** (SPEC §2). All drawing already goes through normal `paint()`, so behavior is identical; the context is purely an acceleration layer for the heavy Phase 5 visualizers.
- 2026-07-02 · P1 · **Test voice = polyBLEP saw** (SPEC/PHASES say only "band-limited saw"): naive saw with a 2-sample polynomial correction at the wrap. Good-enough band-limiting for a temporary voice; the mip-mapped wavetable engine replaces it in Phase 2. Velocity maps to level as `0.2 * (0.3 + 0.7 * vel)`.
- 2026-07-02 · P1 · **`lumen_render` writes 32-bit float stereo WAV**; `--analyze` currently emits `peak_dbfs`, `rms_dbfs`, `dc_offset`, `nan_count` (+ `schema_phase: 1`). The remaining SPEC §18 metrics land in Phase 2 with the real engine.
- 2026-07-02 · P1 · **Phase 1 editor is a placeholder**: header + the 8 frozen parameters and master gain as labelled knobs, SPEC §14 palette, 1040×660 resizable 70–200% with fixed aspect. Full Play/Deep views are Phase 5.
- 2026-07-02 · P2 · **Basic table layout**: pure sine/triangle/saw/square at frames 0/21/42/63, linear harmonic-domain crossfades between them, everything in sine phase so morphing never cancels. "Saw" in the acceptance renders = morph 42/63 ≈ 0.6667 (`lumen::factory::kBasicSawMorph`).
- 2026-07-02 · P2 · **PWM table sweeps duty 50%→95%**; the pulse's DC term is dropped (all factory tables are DC-free). **Harmonic Rise** uses saw-style 1/h amplitudes (frame f = harmonics 1..f+1). **Formant** = two log-space Gaussian peaks (centers 2→24 and 6→90, σ 0.35, second at 0.6 amplitude) over a faint 1/h^1.5 bed so early frames keep a fundamental.
- 2026-07-02 · P2 · **Frame normalization**: each frame peak-normalized via its level-0 peak; all mips of a frame share that one factor so mip crossovers stay seamless.
- 2026-07-02 · P2 · **`Image` table choice already exists in the APVTS** (keeps the choice parameter's normalized mapping stable forever) and resolves to Basic until Lens lands in Phase 6.
- 2026-07-02 · P2 · **Unison law**: lanes spread evenly across ±detune cents; blend crossfades a center-weighted triangle profile against its inverse, floored at 0.05 (avoids silence/div-by-zero at unison 2 + blend 0); gains normalized by the **amplitude sum**, so unison can never exceed one lane's coherent peak (this is why the unison-8 gate passes with margin; revisit toward power normalization post-v1 if unison feels quiet). Width pans lanes ±offset·width, equal-power.
- 2026-07-02 · P2 · **Phase randomize defaults ON** (both oscs). All "randomness" = xorshift64*/xorshift32 PRNGs seeded with fixed constants at `prepare` — renders are bit-deterministic; no `rand()`/`time()` in the Engine.
- 2026-07-02 · P2 · **Velocity → amplitude linear** (vel/127). Curves can come later via the matrix.
- 2026-07-02 · P2 · **Envelope shapes**: attack = x^p, p = 2·3^(−curve) (default x², measured time-to-90% ≈ −5%, inside the ±10% gate); decay/release remaining = (1−x)^q, q = 3^(−curve) (default linear → −60 dB at 0.999·T); releasing voices end below −90 dB. Envelope times update at block rate (an increment change mid-stage can't click).
- 2026-07-02 · P2 · **Resonance mapping Q = 0.5·24^res**, k floored at 1/12. **LP24's second cascaded stage runs fixed Butterworth damping (k = √2)** so its resonance peak matches LP12 instead of doubling in dB. **BP12 output is k·v1** (unity peak gain).
- 2026-07-02 · P2 · **Drive compensation is measured at `prepare`**: one sine cycle at −12 dBFS through tanh(10^(dB/20)·x) for each dB 0..24, RMS ratio stored in a 25-entry table the voice interpolates. Small signals keep loudness; pushed signals saturate without a level jump.
- 2026-07-02 · P2 · **Sub**: tracks Osc A semi+fine pre-unison-detune (SPEC §5); polyBLEP square, naive triangle (1/h² harmonics, negligible aliasing for a sub), exact sine. **Noise level is a dB parameter** (−60 = off .. 0 dB) because SPEC §13's chroma mapping is written in dB; pink = Paul Kellet economy filter.
- 2026-07-02 · P2 · **Filter mode defaults to LP24** (warm classic-subtractive default; spec names no default).
- 2026-07-02 · P2 · **Smoothing**: all continuous engine params via 20 ms SmoothedValues. Morph/level/sub/noise/cutoff/res/drive/env-amount become per-sample buffers; pitch (fine/detune) and pan/width/blend are smoothed then linearly ramped across each block inside the voice (per-sample exp2 would be wasted cost; the ≤block-length ramp of an increment is inaudible).
- 2026-07-02 · P2 · **Harness envelope metrics** ride a per-period peak grid with interpolated crossings; the acceptance timing runs use MIDI 93 so one analysis window (one period, 0.57 ms) is ≤6% of the shortest 10 ms gate. Attack = time to 90% of peak; release = note-off to −60 dB.
- 2026-07-02 · P2 · **--bench chord** = C2 G2 C3 G3 C4 E4 G4 C5, vel 100, held 8 s of 10. Bench peak lands above 0 dBFS pre-limiter by design (float WAV; limiter arrives in Phase 4).
- 2026-07-02 · P2 · **Voice stealing** (quietest-releasing, else oldest) retriggers envelopes from their current level (declick) and keeps the stolen voice's filter state; only fresh voices reset the filter.
- 2026-07-02 · P3 · **Shared range table**: modulation combines in normalized space (SPEC §9), so `Engine/ModDestinations.h` holds one {min, max, skew-centre} table per destination that both the engine's normalize/denormalize and (by convention) Parameters.cpp's NormalisableRanges follow — host mapping and engine mapping cannot drift apart.
- 2026-07-02 · P3 · **Destination set** = every continuous engine parameter (36: filter ×5, osc ×7 each, sub/noise levels, env ADSR+curve ×3). **Macros are excluded as destinations** (they're sources; macro-modulating-macro recursion parked). `delayMix`/`reverbMix`/`masterGain` become destinations in Phase 4 when the engine consumes them.
- 2026-07-02 · P3 · **Application rates per SPEC §9**: global sources (macros, wheel, aftertouch, bend, mono LFOs) are combined per block and land on the existing 20 ms smoothers; poly sources (envs, poly LFOs, velocity, keytrack, random) are combined per voice per block and linearly ramped across the block (per-sample scratch overrides for buffered destinations, start/end shifts for block-ramped ones). Poly source values are sampled at block start — modulation trails its source by ≤ one block (~10 ms), which is what keeps it click-free.
- 2026-07-02 · P3 · **Macro mapping semantics**: {targetParam, min, max} contributes an additive normalized offset `min + (max−min)·macroValue` under the same clamp rule as matrix slots (min = offset at macro 0). Additive keeps macros composable with the base knob and the matrix.
- 2026-07-02 · P3 · **LFOs are bipolar −1..+1**; poly retrigger restarts at the phase-offset parameter; fade-in applies to poly mode only (a free-running mono LFO has no meaningful fade anchor). S&H draws from seeded xorshift32 per LFO instance — fully deterministic. Sync divisions ordered straight/dotted/triplet per base (24 choices), default 1/4.
- 2026-07-02 · P3 · **Source scaling**: velocity, wheel, aftertouch, envs, macros = unipolar 0..1; LFOs, pitch bend, random-per-note = bipolar −1..+1; keytrack = (note−60)/64 clamped to ±1.
- 2026-07-02 · P3 · **Pitch bend now bends pitch** (hard-wired ±2 semitones, smoothed; the 0–24 range setting arrives with the settings panel) *and* is a matrix source.
- 2026-07-02 · P3 · **Matrix/macro state lives in ValueTree children** (`MODMATRIX`/`MACROS`) of the APVTS state with string tokens for source/dest (survives enum growth); published to the audio thread via an 8-deep round-robin pool + atomic pointer swap (no locks; a stale read would need 8 edits inside one block render).
- 2026-07-02 · P3 · **Env-time modulation is block-rate** (attack/decay/sustain/release/curve): only the segment increment changes mid-stage, which cannot click; sustain steps at ≤10 ms resolution are accepted for v1.
- 2026-07-02 · P3 · **Harness metrics**: `--measure-mod` measures modulation rate as the autocorrelation peak of the spectral-centroid trajectory (Hann 2048/hop 256), and `zipper_ratio` = max/median of per-10 ms first-difference peaks on a sine-voice render (smooth sweep ⇒ ~1, parameter steps spike it).
- 2026-07-02 · P3 · **Temporary matrix panel**: 24 plain rows (enable / source / destination / depth) writing straight into the ValueTree; macro mapping lists are harness-and-preset-only until the real macro UX (Phase 5/7).
- 2026-07-02 · P4 · **FX bus lives in the Engine** (`FxChain`, owned by `SynthEngine`, runs after the voice sum) so `lumen_render` exercises the full SPEC §10 path. Master gain moved from the processor into the chain (spec order: limiter → master gain → meter); the processor now only reports latency (`setLatencySamples`).
- 2026-07-02 · P4 · **Per-effect bypass = 20 ms smoothed dry/wet crossfade** whose dry leg is bit-exact (`out = in + mix·(wet − in)`), so the all-bypassed chain nulls at −144 dBFS (measured). Effects keep writing their delay lines while bypassed — re-enabling is instant and click-free. Defaults: drive & chorus OFF, delay & reverb ON (the frozen `delayMix 0` / `reverbMix 0.12` defaults keep init silent/subtle).
- 2026-07-02 · P4 · **Drive**: ±6 dB tilt = one-pole low/high split at 800 Hz applied **pre**-tanh (tone shapes what saturates); loudness compensation reuses the measured −12 dBFS-sine RMS table, now shared as `FxChain::measureTanhCompensation` with the filter drive.
- 2026-07-02 · P4 · **Chorus**: both lines centred at 13.5 ms, ±6.5 ms · depth (spec's 7–20 ms window), right LFO in quadrature (+90°), cubic Hermite interpolation, linear dry/wet.
- 2026-07-02 · P4 · **Delay**: sync shares the LFO division table (default ON, 1/4); synced times beyond the 2 s buffer clamp to 2 s (4/1 at 120 BPM would be 8 s). Time changes glide through a 100 ms smoother → tape-style pitch swoop instead of clicks (also what delay-time modulation sounds like). Ping-pong: mono input feeds L, feedback crosses L→R→L. Damping = one-pole LP (1–16 kHz) inside the feedback path.
- 2026-07-02 · P4 · **Reverb**: `juce::dsp::Reverb` run wet-only with `wetLevel = 1/3` to cancel JUCE's internal 3× wet scale; dry/wet is our own linear mix so `reverbMix` behaves sanely as a mod target.
- 2026-07-02 · P4 · **Limiter** is custom lookahead (not `juce::dsp::Limiter`, which has no lookahead): per-sample required gain → sliding-window minimum → 80 ms one-pole release → boxcar average over exactly the 72-sample (1.5 ms) window. The math guarantees peak ≤ ceiling by construction; a final clamp backstops it. Gain is exactly 1 below the ceiling → bit-transparent (measured −144 dBFS diff). Window min is brute-force (72 samples ≈ 3.5 M ops/s — irrelevant; bench 49× realtime).
- 2026-07-02 · P4 · **+14 FX mod destinations** (append-only): delay/reverb mix, master gain, drive amount/tone, chorus rate/depth/mix, delay time/feedback/damp, reverb size/damp/width. FX destinations are **global-only** — poly-source routings (envelopes, poly LFOs, velocity…) to them are inactive in v1 because the FX bus has no per-voice identity. Delay-time modulation is ignored while sync is on (the division wins).
- 2026-07-02 · P4 · **Meter tap** = per-block peak/RMS struct on the FxChain (`SynthEngine::meterLevels()`); the SPEC §10 lock-free FIFO to the UI arrives with the Phase 5 meter widget.
- 2026-07-02 · P4 · **Harness**: `--no-fx` renders the pre-FX voice sum; `--analyze` trims the 72-sample FX latency before timing metrics (attack/release stay aligned with note events); `--measure-echo` reports echo spacing via 1 ms envelope onsets (interpolated 25 %-of-peak crossings, median of gaps). Default-patch `release_ms_measured` now reads ≈ 0.94 s because the default reverb tail passes −60 dB after the 200 ms envelope — use `--no-fx` (reads 199 ms) or `reverbEnabled=0` for envelope timing work.
- 2026-07-02 · P5 · **Inter 4.1 static TTFs (Regular/Medium/SemiBold) embedded via BinaryData**; the SIL OFL license text is committed at `Assets/Fonts/InterLicenseOFL.txt` *and* embedded alongside the fonts, satisfying SPEC §14's "verify the license file ships with it".
- 2026-07-02 · P5 · **Live mod-arc values are block-rate**: the engine publishes, per block, the final normalized value of every mod destination (base + global matrix + macros + the *newest active voice's* poly sums) through an atomics struct (`Engine/UiTap.h`). Arcs/visualizers show the newest voice, not a per-voice fan-out; values freeze at their last state when the host stops calling processBlock.
- 2026-07-02 · P5 · **UI resizing = one fixed 1040×660 layout scaled by an AffineTransform** (70–200 %, aspect locked). All layout code is in base pixels; text/vector art scale crisply through the transform.
- 2026-07-02 · P5 · **OpenGL context attached in normal runs; skipped for `--screenshot`/`--check-params`** (`LumenAudioProcessorEditor::disableOpenGL`). All drawing is plain `paint()` so both paths are pixel-identical by construction (SPEC §2); snapshots always use the software renderer.
- 2026-07-02 · P5 · **Shift = fine drag** is implemented as JUCE velocity-mode swell on the Shift modifier (slow mouse ≈ pixel-precise) rather than a literal ×0.1 scale — smooth mid-drag transitions, no value jumps.
- 2026-07-02 · P5 · **Right-click knob menu** ships the modulation half of the SPEC §14 contract now (list/add/edit-depth/remove slots, reset-to-default); *MIDI Learn / Clear MIDI* items are present but disabled until Phase 7. Macro *mapping lists* (min/max per target) remain preset/harness-editable only; macro modulation still shows in the knob arcs.
- 2026-07-02 · P5 · **Drag-drop modulation** grabs the first fully-free matrix slot (else the first disabled one) and creates it with depth +0.25. Footer grab handles cover Env 1–3, LFO 1–3, Macro 1–4; the MIDI-ish sources (velocity, wheel, aftertouch, bend, keytrack, random) are reachable via the right-click "Add modulation" menu instead of chips.
- 2026-07-02 · P5 · **Play-view context visualizer**: wavetable stack of Osc A; Osc B's stack when only B is enabled; output scope when both oscs are off. The image view takes over in Phase 6.
- 2026-07-02 · P5 · **Preset strip and Lens card are placeholders** ("Init", arrows disabled / drop-zone text) until Phases 7 and 6 respectively.
- 2026-07-02 · P5 · **On-screen keyboard → engine goes through a lock-free FIFO** on the processor (message thread writes, audio thread applies at block start). Incoming hardware MIDI does not light the on-screen keys in v1.
- 2026-07-02 · P5 · **Frame/dropout instrumentation**: HUD toggles with the `H` key; while on, the editor forces full-window repaints at 60 Hz and measures the whole paint pass (worst-case full redraw). "Dropout" = a `processBlock` whose wall time exceeds its real-time budget, measured with two high-resolution tick reads per block (userspace counter read — accepted RT practice). `Lumen.exe --stress N` automates the Phase 5 gate: real WASAPI device, held 8-note chord, 6 random continuous-parameter jumps per frame at 60 Hz, JSON stats, exit 0 only if avg ≤ 16.7 ms and dropouts = 0. `masterGain` is excluded from wiggling (hearing protection); `--check-params` prints unreachable APVTS parameters as JSON.
- 2026-07-02 · P5 · **Envelope editor time axis**: segment widths = 0.05 + 0.9·√(t/tmax), normalized against a fixed 0.30 sustain shelf — short times stay grabbable, breakpoint drags invert the same mapping. Filter node drag: x = cutoff (log), y-delta = resonance.
- 2026-07-02 · P5r1 · **Incoming MIDI now lights the on-screen keyboard** (supersedes the earlier "does not light in v1" note): the audio thread publishes note on/offs from `handleMidiMessage` into a second lock-free FIFO (`readMidiDisplayEvents`); the editor drains it at 60 Hz into the `MidiKeyboardState` (normalized to display channel 1), with a re-entrancy guard so mirrored notes are not re-sent to the engine. All-notes-off/all-sound-off clears the display. Harness: `--screenshot ... --notes 48,55,60` pumps a held chord through `processBlock` — the identical code path hardware MIDI takes — so the lit keyboard and live scope are verifiable from a PNG.
- 2026-07-02 · P5r1 · **Play-view visualizer context rule extended** (review round 1): whenever audio is active — any voice sounding, or the output meter above ≈ −66 dBFS (FX tails) — the big visualizer shows the live scope, held for 1 s after silence to avoid flicker between notes. The idle rule (Osc A stack / B stack / scope) is unchanged.
- 2026-07-02 · P5r1 · **+3 mod destinations `lfo1Rate`–`lfo3Rate`** (append-only; SPEC §9 says every continuous knob is modulatable — these were missing). Global-only like the FX bus. The modulated rate lands one engine chunk (≤ maxBlock samples) late because LFO values are read before that chunk's sums exist; free-run rate only — a host-synced LFO ignores rate modulation (the division wins). No smoother needed: rate changes alter only the phase increment, so phase stays continuous.
- 2026-07-02 · P5r1 · **Init patch ships default modulation** (SPEC pillar 1 / §14, review round 1): matrix slot 1 = LFO1 → Osc A morph, depth +0.25 (the built-in motion), plus macro maps — Tone → cutoff (−0.60..+0.60) + filter drive (−0.25..+0.25); Motion → LFO1 rate (−0.20..+0.20 ≈ 0.08–7 Hz around the 1 Hz base); Space → reverb mix (−0.195..+0.455) + delay mix (−0.12..+0.28); Texture → Osc A detune (−0.10..+0.40) + noise level (−0.10..+0.40). Every range sums to a **zero offset at the frozen macro defaults** (0.5/0.5/0.3/0.2), so the approved Init timbre is untouched at rest. "Motion controls LFO *depth*" is not literally possible in v1 — matrix slot depths are not macro-modulatable — so Motion drives the wobble **rate**; logged as an approved-pending deviation. Defaults are applied only when the MODMATRIX/MACROS nodes are freshly created; loaded states (even with all slots/maps cleared) are never re-seeded. `lumen_render --preset init` applies the same defaults unless any explicit `--mod`/`--macro` flag is given (measurement isolation for the Phase 3 gates).
- 2026-07-02 · P5r2 · **Macro maps gain an optional response curve** — `{targetParam, min, max, curve}`, offset `= min + (max−min)·macro^curve`, curve 1 = linear (SPEC §9 defines only {min, max}; the round-2 ear pass asked for reverb to arrive earlier in the Space travel, which a linear map pinned to zero-at-0.3 provably cannot do together with a dramatic max). Clamped 0.25–4 on load; a saved state without the `curve` attribute reads as 1, so every pre-existing preset/DAW state is bit-identical. Endpoint-only UI consumers (arc spans, has-modulation checks) are unaffected because the curve is monotone.
- 2026-07-02 · P5r2 · **Macro drama retune** (round-2 ear pass: ranges too conservative). Motion: built-in LFO1→morph slot depth 0.25 → **0.50** (bipolar LFO on a 0-base morph clamps to 0..depth, so the audible wobble doubles). Space: reverb mix −0.12382631..+0.72617369 **curve 1.6** (mix ≈ 0.85 at max vs 0.575 before; wet leaves zero at ~3 % travel vs ~12 %) + delay mix −0.08012291..+0.46987709 curve 1.6 (mix ≈ 0.47 at max); min = −span·0.3^1.6 keeps the zero-at-default pin. Texture: noise −0.15..+0.60 (ceiling −60+0.6·60 = **−24 dB**, was −36) + detune −0.175..+0.70 (45 ct at max, was 30). All maps still sum to a zero offset at the frozen 0.5/0.5/0.3/0.2 defaults (unit-tested).
- 2026-07-02 · P5r2 · **Init Osc A unison 1 → 4**: `Voice::computeLanes` hard-zeroes the detune offset at unison 1, so the shipped Texture→detune map has been a silent no-op since P5r1. Four lanes at the 10 ct base detune make Texture's spread real and give Init a gentle ensemble at rest — this audibly changes the approved Init timbre and is explicitly flagged for the ear pass.
- 2026-07-02 · P5r3 · **Play view "active" visual is now the 3D spectral waterfall** (WATERFALL_SPEC.md, a faithful port of the user's `vDepth` canvas renderer): pseudo-3D ridgeline surface, 44-row history ring, 120 log-spaced columns 30 Hz–18 kHz, Web-Audio-AnalyserNode emulation (0.8 linear-magnitude smoothing → dB → −100..−30 normalization), neon-yellow palette derived at compile time from one accent hex (`#FAFF00`, half-even-rounded per-channel mix, static_asserts pin the §5 table). The scope remains in the Deep view footer strip. The waterfall also inherits the scope's old both-oscillators-off idle fallback in Play view.
- 2026-07-02 · P5r3 · **Waterfall silence drain**: the post-audio hold shrank 60 → 44 frames — while audio is inactive the view pushes all-zero rows (and decays the analyser state by the smoothing constant) so the surface visibly drains toward the horizon over ~0.73 s, then hands back to the idle visual (WATERFALL_SPEC §6, "never look dead").
- 2026-07-02 · P5r3 · **Waterfall floor labels use the system default monospaced typeface**: the spec says "the UI's existing mono face" but the UI embeds none (Inter Regular/Medium/SemiBold only); embedding a fourth font for three tiny axis labels wasn't justified. Revisit if a mono face ships for other reasons.
- 2026-07-02 · P5r3 · **Waterfall analyser lives in `Source/UI/WaterfallModel.h`** — header-only, juce_dsp + stdlib, zero GUI includes — so `lumen_tests` compiles the column mapping and AnalyserNode emulation headless (WATERFALL_SPEC acceptance 2–3). Painting (projection, opaque-fill occlusion, glow/main strokes, cached floor image) stays in `WaterfallView` (Visualizers.cpp).
- 2026-07-02 · P5r4 · **Neon recolor pass (user-directed deviation from the SPEC §14 accent hexes)**: module accents are now neon pink `#FF2E9F` (Osc A/Sub/Macro 1 — everything formerly orange), neon cyan `#19E3E3` (Osc B/Macro 2 — formerly blue), neon yellow (filter + FX + meter + Macro 3 — formerly green). The yellow is `theme::kAccentRgb = 0xFAFF00`, the SAME single constant the waterfall derives its whole WATERFALL_SPEC §5 palette from (Visualizers.h now aliases it) — no second yellow exists. Each neon is one named constant in Theme.h; retuning is a one-liner.
- 2026-07-02 · P5r4 · **Main knob ring and envelope/LFO card accents recolored to neon yellow** per the user's explicit list — note they were warm-gray `accentMod`, not green as the request assumed. `accentMod` (warm gray) remains on the mod-source chips (ENV/LFO/MAC 4), footer scope strip, Lens placeholder, and Macro 4, preserving the chip↔macro color coding (MAC 1 pink / MAC 2 cyan / MAC 3 yellow via the accent remap).
- 2026-07-02 · P5r4 · **Keyboard**: naturals `#C6C6C6`, sharps `#3A3A3A`, pressed/hover overlay = kAccent neon yellow (the intentional exception to the pink rule — pressed keys share the waterfall yellow). JUCE's default `drawBlackNote` paints the key face `colour.brighter()` (a flat `#3A3A3A` face is unreachable), so `FlatKeyboard` (Cards.h) overrides it with a flat fill + flat down/over overlays. The output meter had no clip/hot zone to preserve — it recolored uniformly to yellow with the white peak-hold marker unchanged; a distinct hot zone stays a possible follow-up.
- 2026-07-02 · P5r4 · **Output meter hot zone** (follow-up to the recolor pass): the top −3 dBFS..0 of the scale is red (`theme::meterHot #FF3B30` — distinct from neon pink) — a faint 0.22-alpha marker on the empty track, the RMS fill segment above −3 dB, and the peak line once peak ≥ −3 dB; everything below stays neon yellow and the white 300 ms peak-hold marker is unchanged. Neon recolor approved by the user, including Main knob + env/LFO = yellow and the four-color macro/chip coding kept for the drag-drop mod system.
- 2026-07-02 · P5 gate · **Phase 5 approved by the user (ear + eye)**: waterfall drain, Motion/Space/Texture macro ranges, neon recolor, and the meter hot zone all confirmed. **Init unison-4 ensemble kept as-is** (4 lanes @ 10 ct base detune — per the "fully approved" ear pass; revisit alongside the Phase 7 factory-bank design if the default ever needs to be drier). Gate evidence this session: full unit suite green, pluginval strictness 10 green, Play-view stress 5.08 ms avg / 6.38 ms worst / 0 dropouts, screenshots pixel-verified.
- 2026-07-02 · P6 · **Lens layer layout**: `Source/Lens/LensEngine` (pure analysis: decode/downscale, FNV-1a seed, scan/spectral frame builds, chroma stats — juce_graphics + juce_dsp + juce_cryptography, all JUCE) is shared by plugin, `lumen_render`, and `lumen_tests`; `Source/Lens/LensController` (table ownership, state writes, chroma patch application) is plugin-only. The Engine layer stays byte-for-byte GUI-free — Lens tables enter `SynthEngine` only as `const Wavetable*`. Render/tests targets now also link `juce_cryptography` (SHA-256) and, explicitly, `juce_graphics` (was already transitive via `juce_audio_processors`).
- 2026-07-02 · P6 · **Image-table swap & retire protocol** (CLAUDE.md rule 1): per-osc `std::atomic<const Wavetable*>` in the engine, swapped on the message thread; `SynthEngine::render` bumps a counter, and `LensController` frees a retired table only once the counter has advanced ≥ 2 past the swap (any render concurrent with the swap has finished by then). If the host never renders, retired tables just wait (bounded by loads per session; ~5 MB each).
- 2026-07-02 · P6 · **Spectral phases are per-harmonic, shared across all 64 frames**: one xorshift64* stream seeded by the image's FNV-1a hash deals 256 phases; frames differ only in amplitude. Per-frame phases would make the equal-power frame crossfade comb/cancel while morphing. The scan-mode silence guard (`0.05·sin` when peak < 1e-4) also protects spectral frames from all-black columns.
- 2026-07-02 · P6 · **Chroma stat definitions where SPEC 13.5 is loose**: Hm = saturation-weighted circular mean (gray pixels have no hue and must not drag the mean toward red; all-gray → weight 0 → Hm 0 → LP24); sigH = circular variance (1 − R) with the same weights; sigV = 2·stddev(luma) clamped 0..1 (max-contrast image ≈ 1); E = mean Sobel magnitude / 4 (per-axis kernel max) clamped 0..1.
- 2026-07-02 · P6 · **“Drive = 20·E dB” maps to the Drive EFFECT** (`driveAmount`, auto-enabled when > 0.05 dB), matching SPEC §10’s capital-D Drive; filter drive stays user territory. Chroma’s detune/unison/LFO-morph land on the **Lens target osc** (SPEC text says Osc A; generalized so targeting B behaves symmetrically).
- 2026-07-02 · P6 · **Lens settings (Scan/Spectral, colors toggle, A/B target) are LENS-tree properties, not APVTS parameters** — they configure an offline analysis action, not the audio path, and shouldn’t be host-automatable. Persisted with the patch. Wavetable frames stored as standard Base64 float32 LE inside the LENS tree (~683 KB per osc; x64-only per SPEC §2), thumbnail as Base64 PNG. Verified: a 700 KB XML attribute round-trips fine through ValueTree/XML.
- 2026-07-02 · P6 · **Mode toggle re-analyzes only in-session**: the decoded source stays in RAM for the session, so flipping Scan/Spectral (or re-targeting + dropping again) rebuilds instantly; after a state reload only the generated table + thumbnail exist (SPEC 13 stores no source), so the new mode applies to the next drop. The panel shows the working image in-session and the 64×64 thumbnail after reload. Turning “COLORS” on with an image already loaded applies the chroma patch immediately.
- 2026-07-02 · P6 · **“Gradient” test image = spatial-frequency chirp** (512×64, row r holds r+1 sine cycles): a literal luminance gradient has constant rows, which DC removal turns into the silence guard on every frame — the centroid gate would measure a constant. The chirp makes scan frame i ≈ harmonic i+1 (measured centroid 10.7 → 154.4, monotonic). Stripes = 3 px white lines at the exact log-map y of harmonics {2,4,8,16,32,64} (3 px so the exact harmonic’s 3×3 patch saturates → strict peaks); checker = 32 px cells (zero saturation, strong edges) for chroma.
- 2026-07-02 · P6 · **UI**: images drop anywhere on the editor (`FileDragAndDropTarget` on the editor itself; yellow border + banner while dragging, 2.5 s result banner after). The scanline is coloured by the target osc’s accent (pink A / cyan B) — horizontal traveling down in Scan, vertical in Spectral, driven by the same live-morph rule as the 3D stack (tap value once morph is modulated). The Play-view drop zone is now the real Lens panel; the big Play visualizer shows the image view when the shown osc plays its Image table (idle), waterfall still owns “audio active”. Deep card mode tabs read SCAN/SPEC (96 px column).
- 2026-07-02 · P6 · **Harness**: `lumen_render --image <file> --mode scan|spectral --lens-target A|B [--chroma]` prints `lens_seed` + `lens_table_sha256` and adds lens/chroma fields to the analyze JSON; `--gen-image gradient|stripes|checker --out x.png` writes the procedural images; `Lumen.exe --screenshot ... --lens-image x.png` loads an image before the snapshot. Reference checksums pinned in `lumen_tests`: scan `cdb17834…`, spectral `62a3e9c6…` (identical in-memory, from PNG files, and across processes).
- 2026-07-02 · P6 polish · **Waterfall restyle (WATERFALL_SPEC.md amended in place)**: row fills are now solid opaque `#070708` (occlusion unchanged, silhouetted ridgelines — the 3-stop gradient and the glow underlay are gone); main stroke fixed 1.0 px at all sizes (same bright→accent depth ramp); `DEPTH_R` 44 → 22 with the ring advancing one row every OTHER 60 fps frame (~30 rows/s × 22 rows ≈ the same 0.73 s of history at half the density — FFT + smoothing also run at the row cadence); a synthwave perspective floor grid joined the cached resize-only floor image (horizontals at each row baseline spanning that row's width, verticals at the 1-2-5 tick positions converging front→horizon at the 0.58 back scale, accent @ 0.12 alpha within the spec'd 0.10–0.15). Baseline, ticks and 100/1k/10k/Hz labels unchanged.
- 2026-07-02 · P6 polish · **Play view context switching removed — the waterfall scene is permanent** (supersedes the P5r1 scope-context rule and the P5r3/P6 idle-visual handoffs): idle = grid + drained flat surface, playing = ridges rise, release = drains in place; the wavetable-stack and big-image contexts are gone from the Play view (Deep view cards and the Lens panel are untouched). Once the ring is fully drained the scene stops repainting (static image) until audio returns — idle paint cost is ~zero.
- 2026-07-02 · P6 polish · **Lens panel affordances**: empty state is a dashed drop zone with a "+" — clicking it opens a native async file chooser (`*.png;*.jpg;*.jpeg;*.gif`, same set the window-wide drag-drop accepts, which stays); loaded state gains an "×" (remove) and a replace icon, and clicking the thumbnail re-opens the chooser. Remove (`LensController::removeImage`) clears the session image + engine table (atomic swap + retire protocol) + the persisted LENS IMAGE node (frames/thumbnail/name), and reverts the target osc's Table to `Basic` (the Init wavetable) *only if* it is still on `Image`; every other patch parameter is left as-is.
- 2026-07-02 · P6 polish · **Evidence for the polish round**: unit suite ALL PASSED (incl. new warm/busy macro-separation and lensstate::removeImage tests); Play-view stress with the permanent waterfall 2.30 ms avg / 3.29 ms worst / 0 dropouts (was 5.08/6.38 at the P5 gate — solid fills + half-rate advance + 22 rows more than repay the grid); pluginval strictness 10 SUCCESS; `--check-params` 96/96; screenshots verified (Play idle grid + drained surface, Play chord ridgelines, Lens panel empty dashed drop zone + loaded with ×/replace buttons, Deep card ditto). User look/listen sign-off pending.
- 2026-07-02 · P6 polish · **Chroma → macro knob positions (SPEC 13.5 extension; supersedes "Macro 4 (Texture) default = E")**: when COLORS is on and an image is applied, the four macros are also set from the image statistics as `macro = clamp01(initDefault + 0.7·(stat − 0.5))` with initDefaults {0.5, 0.5, 0.3, 0.2}: Tone ← Vm, Motion ← sigV, Space ← 1 − E′, Texture ← E′, where `E′ = min(E / 0.25, 1)` rescales the practically small Sobel mean (real photos rarely exceed ~0.25) onto the full axis. Gain 0.7 ≈ the requested "default ±0.35" conservative travel (constants `lens::kMacroGain`, `lens::kEdgeFull`, `lens::kMacroDefaults`). Verified with the new deterministic photo stand-ins `testimages::warm()`/`busy()` (also `--gen-image warm|busy`): warm-soft lands Tone 0.798 / Motion 0.271 / Space 0.643 / Texture 0.000, busy-high-contrast lands Tone 0.633 / Motion 0.632 / Space 0.000 / Texture 0.550 — unit-gated at ≥ 0.15 separation on Motion/Space/Texture. `lumen_render --image` now emits `lens_macro_tone/motion/space/texture` in the analyze JSON.
- 2026-07-02 · P6 gate · **Evidence this session**: unit suite ALL PASSED (determinism + pinned SHAs, state round-trip bit-exact incl. re-rendered audio, centroid monotonic 10.7→154.4, stripe peaks exactly {2,4,8,16,32,64}, chroma solid-color exact values, checker stats); two-process file-level determinism + byte-identical WAVs; pluginval strictness 10 SUCCESS; `--check-params` 96/96; stress 15 s play 5.15 ms avg / deep 6.30 ms avg, 0 dropouts; screenshots verified (deep card + play panel with image and scanline, Osc A stack showing the image table).
- 2026-07-03 · P7 · **Factory preset architecture**: the 32-preset bank is defined once in code (`State/FactoryPresets.cpp`, a patch-sheet builder: settings in natural units + matrix routes + macro maps + optional Lens spec) and applied through two paths — `applyToEngine()` (lumen_render/tests, EngineParams) and `buildState()` (plugin, a complete APVTS state tree for `replaceState`). A unit test renders every preset through both paths and requires bit-identical audio. Factory presets live in memory (always in the browser, never written to disk); user presets are `.lumen` files (state XML) at `Documents/Lumen/Presets/<Category>/<Name>.lumen`, scanned by `PresetManager`. Factory names win filename collisions. `lumen_render --preset` resolves factory names first, then treats the argument as a `.lumen` path.
- 2026-07-03 · P7 · **Category directory/display name is "Keys"** for SPEC's "Keys/Plucks" (slash is illegal in folder names). Display order: Bass, Leads, Pads, Keys, Textures.
- 2026-07-03 · P7 · **Preset metadata lives on the state root** (`presetName`/`presetCategory`/`presetAuthor` properties), so the browser caption and DAW session recall always agree. Loading goes through one path: `LumenAudioProcessor::loadPresetState` == the `setStateInformation` body.
- 2026-07-03 · P7 · **Default patch = Neon Tide, loaded in the processor constructor** (hosts overwrite it via setStateInformation on session restore). Parameter *defaults* (double-click reset) intentionally remain the Init values — reset returns a knob to neutral, not to the loaded preset.
- 2026-07-03 · P7 · **`rms_held_dbfs` metric added to `--analyze`** (stereo RMS from note-on to note-off, latency-aligned): the SPEC "RMS −20..−8 dBFS at velocity 100" gate is measured on this window — whole-buffer RMS would punish short envelopes for their silent tail. Verification protocol per category: Bass note 36 (dur 1.5 s), Leads 72 (2 s), Pads 60 (3 s), Keys 72 (0.6 s "pluck gesture" window; Dial Tone/Soft EP at 60), Textures 60 (3 s). Sanity gates beyond SPEC: attack ≤ 30 ms (Bass) / ≤ 80 ms (Keys; Marble Pluck's 54 ms 90%-of-peak time is its resonant env-2 sweep, onset is 1 ms) / ≤ 300 ms (Leads) / ≥ 100 ms (Pads); release ≤ 1 s (Bass) / ≤ 2 s (Keys) — measured release includes the reverb tail, not just Env 1 — and ≥ 0.7 s (Pads). All 32 green (table in the phase report).
- 2026-07-03 · P7 · **Macro map convention**: each map = `{dest, span, curve}`; min/max derive from the preset's stored macro position `p` as `min = −span·p^curve`, so every knob RESTS sound-neutral on the designed patch and sweeps away from it (the Init convention generalized). All presets keep the frozen rest positions 0.5/0.5/0.3/0.2; Space maps reuse Init's curve 1.6. Every preset maps all four macros (unit-gated, strongest span ≥ 0.15).
- 2026-07-03 · P7 · **Lens factory presets (`Photograph`, `Scanline`) don't use the chroma map**: Lens supplies only the wavetable (gradient/Scan and stripes/Spectral of the Phase 6 procedural images); the patches around the tables are hand-designed. Frames + thumbnail are stored in the LENS state tree exactly like a user drop — determinism and bit-exact recall ride the Phase 6 machinery. Photograph keeps its morph travel in the low frames (base 0.08, LFO ±0.12): the gradient's higher frames are single high harmonics that the LP filter silences.
- 2026-07-03 · P7 · **Loudness lessons baked into the bank**: FX mixes are linear dry/wet crossfades, so stacked chorus+delay+reverb mixes cost ~6–10 dB of dry level; unison is amplitude-normalized (≈ −4.8 dB RMS at 4–7 lanes vs one). Presets compensate with per-preset `masterGain` trims (≤ +6 dB) and keep peaks ≤ ≈ −1.4 dBFS. Observed once during tuning: `juce::dsp::Limiter` can overshoot its −0.3 dBFS ceiling (+0.5 dB) on ~1 ms transients despite the lookahead — presets keep transient crest headroom instead of leaning on the limiter.
- 2026-07-03 · P7 · **SPEC section 11 voice modes (poly/mono/legato) and glide were never implemented** (no APVTS parameters exist) — surfaced during preset design (no mono-bass/glide-lead patches possible). Flagged to the user for the remainder of Phase 7; presets are poly-only for now.
- 2026-07-03 · P7 (partial) · **Gate evidence this session (preset bank scope)**: unit suite ALL TESTS PASSED (98 groups; new: bank shape/validity, per-preset engine-vs-state bit-identical renders, APVTS save/recall bit-exact fixed point, Lens frame round-trip, .lumen file IO, unknown-key tolerance); all 32 presets render clip-free with held RMS −8.6..−19.5 dBFS at velocity 100, nan 0; pluginval strictness 10 SUCCESS; screenshots verified (header strip "Neon Tide" + browser buttons, deep view reflects the default patch, play view silent at idle). Remaining Phase 7 items for the next session: MIDI Learn + persisted map, standalone settings persistence, README manual, tooltips/strings pass, v1.0.0.
- 2026-07-03 · P7 polish · **UI/branding pass (no DSP, no APVTS)**: five changes, `--check-params` still 96/96, unit suite ALL PASSED, pluginval strictness 10 SUCCESS, Play-view stress 1.85 ms avg / 3.21 ms worst / 0 dropouts.
  1. **Lens COLORS defaults ON**: `lensstate` chroma property default flipped `0 → 1` (both `ensureTree` and the `chroma()` getter fallback). Effect is scoped to bare/init state — a fresh Lens tree now sets patch-from-colors on the first image drop. No-image sound is unchanged (chroma is only read in `applyChromaPatch`, called solely on image load / the toggle, never on state load or idle) and the 32 factory presets are untouched (each bakes an explicit `setChroma(false)` in `FactoryPresets::buildState`, so the default never reaches them).
  2. **Wordmark tracking**: `theme::drawTrackedText` draws one styled string glyph-by-glyph with em-relative inter-letter spacing (uses `GlyphArrangement::getStringWidth` — `Font::getStringWidthFloat` is gone in JUCE 8). Wordmark "LUMEN" at 0.34em; reused for menu section headers at 0.22em.
  3. **Header rework** (shared `HeaderBar`): gear button immediately right of PLAY/DEEP; Main knob + meter kept in order but nudged left to make room; minimize + close (`HeaderIconButton`, close-hover `#FF3B30`) at the far right past the meter. Right cluster: viewTabs 648, gear 774, master 808, meter 864, min 978, close 1006.
  4. **Standalone = borderless**: `LumenStandaloneWindow : StandaloneFilterWindow` sets `setTitleBarHeight(0)`, dropping the JUCE title strip AND its Options button; the header is the title bar (drag an empty region → `ComponentDragger` moves the top-level window; minimize/close drive `getPeer()`). The gear opens `StandalonePluginHolder::showAudioSettingsDialog()` via `LumenAudioProcessorEditor::standaloneSettingsHook` (lazy `getInstance()`), so shared UI never links standalone headers. Standalone-vs-plugin chrome is a runtime decision (`JUCEApplicationBase::isStandaloneApp()`), overridable via `chromeOverride` for screenshots. In the plugin build there are no window controls; the gear opens a minimal branded menu (LUMEN / version / MIDI Learn / Tooltips placeholders).
  5. **Branded popup look** (`LumenMenuLookAndFeel : LumenLookAndFeel`, so `LumenLookAndFeel` lost its `final`): rounded `#151518` panel + `#2a2a2f` border, mono uppercase tracked `#6f6d64` section headers, `#d8d5cc` items, hover row `rgba(250,255,0,0.10)`, ticked/current item `#FAFF00`, hairline separator before "Save Preset...". Set on both the preset browser and the gear menu via `PopupMenu::setLookAndFeel`.
- 2026-07-03 · P7 polish 2 · **Popup menus are per-pixel transparent**: `LumenMenuLookAndFeel` sets `PopupMenu::backgroundColourId` to transparent, which makes JUCE's MenuWindow non-opaque (it calls `setOpaque(colour.isOpaque())`), so the corners outside the rounded `#151518` panel show what's behind the menu instead of opaque white. Verified by compositing the (ARGB) harness-rendered menu over bright `#E9E9EC` and dark `#0F0F12` backdrops (`--menu-bg bright|dark`).
- 2026-07-03 · P7 polish 2 · **Preset browser in 3 columns** (`withMinimumNumColumns(3)` + max 3; the fixed `withMinimumWidth` is gone — columns size to their items). Category headers, current-patch tick and the Save Preset... divider unchanged; the `--menu preset` harness mock mirrors the 3-column flow.
- 2026-07-03 · P7 polish 2 · **Per-preset macro knob positions** (supersedes "All presets keep the frozen rest positions" in the P7 macro-map entry): all 31 non-Init presets store explicit `macro1..4` values chosen to reflect their character (Tone 0.15–0.80, Motion 0.20–0.70, Space 0.10–0.65, Texture 0.10–0.85 across the bank; unit-gated ≥ 6 distinct values and ≥ 0.4 range per macro). `macroMapRange` recalibrates every map around the stored position with a BIT-exact zero offset: a mapped destination always renders `denormalize(normalize(base) + offset)`, so only `offset == 0.0f` exactly keeps the sound identical across recalibrations — the span is nudged an ulp at a time until `min = -(d·shaped)` cancels in float (`mod::macroMapOffset(map, position) == 0.0f`, unit-gated per map, plus a per-preset render gate: stored-position calibration == frozen-default calibration bit-identically). All 32 preset renders verified byte-identical to the pre-change bank (SHA-256 over WAVs). The ≥ 0.15 strongest-span gate stays.
- 2026-07-03 · P7 polish 2 · **Header**: the master knob lost its "Main" label (`ModKnob` with an empty label uses the full cell) and sits at (810, 5, 38, 38) so knob centre = y 24 = header centre, matching the meter.
- 2026-07-03 · P7 polish 2 · **Keyboard follow**: each UI tick the Play view scans the sounding notes (`MidiKeyboardState`); if ALL are outside the visible 2-octave window it slides whole octaves toward them (C-aligned, clamped 0–120; lowest-note distance rounded up), and never moves while any visible note is held. Yellow edge arrows (dim `textMuted` affordances with a dark outline so they read on lit keys) light neon yellow with a soft halo while notes sound beyond the window on that side.
- 2026-07-03 · P7 polish 2 · **Lens morph journey**: applying a user image also routes `env3 -> osc<target>Morph` (reusing an existing env3->morph slot, else the first free one) with depth `lens::kJourneyDepth = 1.0`, sets env3 attack `kJourneyAttackSeconds = 5 s` / sustain 1 / curve 0, and adds a Motion -> env3Attack macro map (span `kJourneyMotionSpan = -0.5`, pinned neutral at Motion's value at drop time) — every note travels down/through the image; Motion up ≈ 0.4 s full-image sweeps, Motion down ≈ the 10 s attack ceiling. Applied ONLY in `loadImage` (drops + `--lens-image`), never on preset/state loads, so Photograph/Scanline keep their own morph motion un-doubled; `removeImage` takes the route (and the Motion map, if no other osc journeys) back out. Render evidence via explicit flags on the gradient chirp: centroid 594→10573 Hz over 4 s vs static 652→417 Hz; Motion=1 reaches 12639 Hz in 2 s vs 3025 Hz at Motion=0.5.
- 2026-07-03 · P7 polish 2 · **Scanline restyle**: the Lens beam is white (0.95 alpha, 2.4 px, full image width in Scan / height in Spectral) with a 16 px gradient fade trailing behind the travel direction, clipped to the image — reads as a scan beam. The per-osc accent colouring from P6 is gone.
- 2026-07-03 · P7 polish 2 · **Harness**: `--screenshot --settle <ms>` overrides the 700 ms snapshot wait (catches the journey mid-image); `--menu-bg bright|dark` composites the transparent menu render over a backdrop; `--measure-mod` JSON gains `centroid_start_hz`/`centroid_end_hz` (trajectory endpoints — one-way ramps like the journey don't autocorrelate).
- 2026-07-03 · P7 polish · **Screenshot harness**: `--screenshot ... [--chrome plugin|standalone] [--menu preset|gear]`. `--chrome` forces the header style so both the borderless standalone header and the frameless plugin header are captured from the one standalone binary. `--menu` can't snapshot the live async popup headlessly (with no pointer over it the message loop dismisses it before it paints, and a manually-built `Graphics`/`drawImageAt` composite is a no-op in this software-render context), so it renders the branded menu through `LumenMenuLookAndFeel`'s own draw methods (fills/text/paths — those DO work off a fresh image) into an offscreen image — exercising the exact styling code that dresses the real menu.
